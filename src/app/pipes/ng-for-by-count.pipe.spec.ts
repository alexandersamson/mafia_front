import { NgForByCountPipe } from './ng-for-by-count.pipe';

describe('NgForByCountPipe', () => {
  it('create an instance', () => {
    const pipe = new NgForByCountPipe();
    expect(pipe).toBeTruthy();
  });
});
